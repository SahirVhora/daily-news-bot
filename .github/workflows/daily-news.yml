name: Daily UK News

on:
  schedule:
    - cron: "15 6 * * *"   # 6:15 UTC = 7:15 AM BST (UTC+1)
  workflow_dispatch:

jobs:
  send-uk-news:
    uses: ./.github/workflows/_run-digest.yml
    secrets: inherit
    with:
      digest_name: "Daily UK News"
      script_path: "uk_news.py"
